# docker-compose.yml
version: '3.8'

services:
  db:
    image: postgres:15
    volumes:
      - postgres_data:/var/lib/postgresql/data
    environment:
      POSTGRES_DB: devis_factures_db
      POSTGRES_USER: django_user_admin
      POSTGRES_PASSWORD: qwerty12345
    ports:
      - "5432:5432"

  web:
    build: .
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    environment:
      - DEBUG=1
      - DATABASE_URL=postgresql://django_user:django_password@db:5432/devis_factures_db
    depends_on:
      - db

volumes:
  postgres_data: